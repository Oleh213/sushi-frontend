import { OrderDataFormatPipe } from './order-data-format.pipe';

describe('OrderDataFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderDataFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
